return {
  "saghen/blink.cmp",
  opts = {
    completion = {
      accept = {
        auto_brackets = {
          enabled = false, -- turn of auto-parenthesis after accepting a completion item
        },
      },
    },
  },
}
