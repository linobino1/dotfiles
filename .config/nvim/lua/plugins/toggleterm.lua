return {
  "akinsho/toggleterm.nvim",
  version = "*",
  opts = {
    size = 20,
    open_mapping = [[<leader>tt]], -- bind the toggle key
    direction = "float",
    close_on_exit = true,
    persist_size = true,
  },
}
